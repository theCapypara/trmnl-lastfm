{% if errors %}
    {% render "error_info", errors: errors %}
{% else %}
    {% render "content", data: data, num_cols: num_cols["half"], max_albums: max_albums["half"] %}
{% endif %}
{% render "title_bar", data: data, trmnl: trmnl %}
