{% if errors %}
    {% render "error_info", errors: errors %}
{% else %}
    {% render "content", data: data, num_cols: num_cols["quad"], max_albums: max_albums["quad"] %}
{% endif %}
{% render "title_bar", data: data, trmnl: trmnl %}
